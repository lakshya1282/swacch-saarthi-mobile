const mongoose = require('mongoose');
require('dotenv').config();

mongoose.connect(process.env.MONGODB_URI || 'mongodb://localhost:27017/waste-management');

const Pickup = require('./models/Pickup');
const User = require('./models/User');

async function finalBugTest() {
  console.log('🎯 FINAL ASSIGNMENT BUG TEST\n');
  console.log('Testing multiple race conditions simultaneously...\n');
  
  try {
    // Clean up any existing test data
    await Pickup.deleteMany({ pickupId: { $regex: /^FINAL/ } });
    
    const citizens = await User.find({ userType: 'citizen' }).limit(1);
    const workers = await User.find({ userType: 'worker' }).limit(4);
    
    if (citizens.length === 0 || workers.length < 2) {
      console.log('❌ Need at least 1 citizen and 2 workers');
      return;
    }
    
    console.log(`👤 Using citizen: ${citizens[0].firstName} ${citizens[0].lastName}`);
    console.log(`👥 Testing with ${workers.length} workers:`);
    workers.forEach((worker, i) => {
      console.log(`${i + 1}. ${worker.firstName} ${worker.lastName}`);
    });
    
    // Create multiple test tasks
    const testTasks = [];\n    for (let i = 1; i <= 3; i++) {\n      const task = new Pickup({\n        pickupId: `FINAL${Date.now()}${i}`,\n        verificationCode: `FINAL${i}23`,\n        citizenId: citizens[0]._id,\n        customerName: `Test Customer ${i}`,\n        customerAddress: `Azad Chowk - Test Location ${i}`,\n        customerPhone: '9303795369',\n        wasteTypes: ['dry', 'wet', 'hazardous'][Math.floor(Math.random() * 3)] === 'hazardous' ? ['hazardous'] : ['dry', 'wet'],\n        estimatedWeight: `${3 + i}kg`,\n        timeSlot: ['morning', 'afternoon', 'evening'][i % 3],\n        scheduledDate: new Date(Date.now() + (i * 60 * 60 * 1000)), // i hours from now\n        status: 'scheduled',\n        priority: ['medium', 'high', 'urgent'][i % 3]\n      });\n      \n      await task.save();\n      testTasks.push(task);\n      console.log(`📋 Created task ${i}: ${task.pickupId}`);\n    }\n    \n    console.log('\\n🧪 TEST 1: Multiple workers accepting the same task simultaneously');\n    \n    // Test 1: All workers try to accept the same task\n    const task1 = testTasks[0];\n    console.log(`Target task: ${task1.pickupId} - ${task1.customerAddress}`);\n    \n    const promises1 = workers.map((worker, i) => \n      Pickup.acceptTaskAtomically(\n        task1.pickupId, \n        worker._id, \n        `${worker.firstName} ${worker.lastName}`\n      )\n    );\n    \n    const results1 = await Promise.all(promises1);\n    \n    console.log('\\n📊 Results:');\n    let successCount1 = 0;\n    results1.forEach((result, i) => {\n      const status = result.success ? '✅ SUCCESS' : `❌ ${result.error}`;\n      console.log(`${workers[i].firstName}: ${status}`);\n      if (result.success) successCount1++;\n    });\n    \n    console.log(`\\n🎯 Analysis: ${successCount1} worker(s) succeeded`);\n    if (successCount1 === 1) {\n      console.log('✅ PERFECT: Only one worker got the task');\n    } else if (successCount1 > 1) {\n      console.log('🚨 BUG DETECTED: Multiple workers got the same task!');\n    } else {\n      console.log('❓ UNEXPECTED: No worker got the task');\n    }\n    \n    // Check database state\n    const finalTask1 = await Pickup.findOne({ pickupId: task1.pickupId });\n    console.log(`Database state: Status=${finalTask1.status}, Worker=${finalTask1.workerId}`);\n    \n    console.log('\\n🧪 TEST 2: Different workers accepting different tasks simultaneously');\n    \n    // Test 2: Each worker accepts a different task\n    const promises2 = workers.slice(0, Math.min(workers.length, testTasks.length - 1)).map((worker, i) => \n      Pickup.acceptTaskAtomically(\n        testTasks[i + 1].pickupId, \n        worker._id, \n        `${worker.firstName} ${worker.lastName}`\n      )\n    );\n    \n    const results2 = await Promise.all(promises2);\n    \n    console.log('\\n📊 Results:');\n    let successCount2 = 0;\n    results2.forEach((result, i) => {\n      const status = result.success ? '✅ SUCCESS' : `❌ ${result.error}`;\n      console.log(`${workers[i].firstName} → Task ${testTasks[i + 1].pickupId}: ${status}`);\n      if (result.success) successCount2++;\n    });\n    \n    console.log(`\\n🎯 Analysis: ${successCount2} assignments succeeded`);\n    console.log('✅ This should equal the number of different tasks assigned');\n    \n    console.log('\\n🧪 TEST 3: Race condition stress test');\n    \n    // Create one more task for stress testing\n    const stressTask = new Pickup({\n      pickupId: `STRESS${Date.now()}`,\n      verificationCode: 'STRESS123',\n      citizenId: citizens[0]._id,\n      customerName: 'Stress Test Customer',\n      customerAddress: 'High-concurrency test location',\n      customerPhone: '9303795369',\n      wasteTypes: ['dry', 'wet'],\n      estimatedWeight: '10kg',\n      timeSlot: 'morning',\n      scheduledDate: new Date(Date.now() + 30 * 60 * 1000), // 30 minutes from now\n      status: 'scheduled',\n      priority: 'urgent'\n    });\n    \n    await stressTask.save();\n    console.log(`📋 Created stress test task: ${stressTask.pickupId}`);\n    \n    // Multiple rapid attempts from the same worker and different workers\n    const rapidPromises = [];\n    \n    // Same worker tries multiple times\n    for (let i = 0; i < 3; i++) {\n      rapidPromises.push(\n        Pickup.acceptTaskAtomically(\n          stressTask.pickupId, \n          workers[0]._id, \n          `${workers[0].firstName} ${workers[0].lastName} (attempt ${i + 1})`\n        )\n      );\n    }\n    \n    // Different workers try simultaneously\n    workers.slice(1).forEach(worker => {\n      rapidPromises.push(\n        Pickup.acceptTaskAtomically(\n          stressTask.pickupId, \n          worker._id, \n          `${worker.firstName} ${worker.lastName}`\n        )\n      );\n    });\n    \n    const rapidResults = await Promise.all(rapidPromises);\n    \n    console.log('\\n📊 Stress Test Results:');\n    let stressSuccessCount = 0;\n    rapidResults.forEach((result, i) => {\n      const isMultiple = i < 3;\n      const workerName = isMultiple ? \n        `${workers[0].firstName} (attempt ${i + 1})` : \n        workers[i - 2].firstName;\n      const status = result.success ? '✅ SUCCESS' : `❌ ${result.error}`;\n      console.log(`${workerName}: ${status}`);\n      if (result.success) stressSuccessCount++;\n    });\n    \n    console.log(`\\n🎯 Stress Test Analysis: ${stressSuccessCount} assignment(s) succeeded`);\n    if (stressSuccessCount === 1) {\n      console.log('✅ EXCELLENT: Race condition handling is perfect');\n    } else {\n      console.log('🚨 CRITICAL: Race condition vulnerability detected!');\n    }\n    \n    // Final summary\n    console.log('\\n' + '='.repeat(50));\n    console.log('🏆 FINAL TEST RESULTS');\n    console.log('='.repeat(50));\n    \n    const allCurrentAssignments = await Pickup.find({ \n      status: 'assigned',\n      pickupId: { $regex: /^(FINAL|STRESS)/ }\n    }).populate('workerId', 'firstName lastName');\n    \n    console.log(`\\n👷 Total assignments created: ${allCurrentAssignments.length}`);\n    allCurrentAssignments.forEach(task => {\n      const workerName = task.workerId ? \n        `${task.workerId.firstName} ${task.workerId.lastName}` : \n        'Unknown Worker';\n      console.log(`📋 ${task.pickupId} → ${workerName}`);\n    });\n    \n    const totalTests = 3;\n    const passedTests = [\n      successCount1 === 1,\n      successCount2 > 0,\n      stressSuccessCount === 1\n    ].filter(Boolean).length;\n    \n    console.log(`\\n🎯 OVERALL SCORE: ${passedTests}/${totalTests} tests passed`);\n    \n    if (passedTests === totalTests) {\n      console.log('\\n🎉 SUCCESS! The assignment bug has been COMPLETELY FIXED!');\n      console.log('✅ Race conditions are properly handled');\n      console.log('✅ Only one worker can accept each task');\n      console.log('✅ Atomic operations are working correctly');\n      \n      console.log('\\n📱 MOBILE APP INTEGRATION:');\n      console.log('1. Ensure mobile app uses task.pickupId (not task._id) in API calls');\n      console.log('2. Clear task cache immediately after acceptance');\n      console.log('3. Listen for Socket.IO real-time updates');\n      console.log('4. Handle 409/conflict errors gracefully');\n      console.log('5. Auto-refresh task lists every 30 seconds');\n      \n    } else {\n      console.log('\\n⚠️ Some issues remain. Check the failed tests above.');\n    }\n    \n    // Clean up test data\n    await Pickup.deleteMany({ pickupId: { $regex: /^(FINAL|STRESS)/ } });\n    console.log('\\n🗑️ Cleaned up test data');\n    \n  } catch (error) {\n    console.error('❌ Final test failed:', error);\n  } finally {\n    mongoose.connection.close();\n  }\n}\n\nfinalBugTest();